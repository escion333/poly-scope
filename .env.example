# Sentry error/stability tracking. Get the DSN from your Sentry project:
# Settings → Projects → <project> → Client Keys (DSN).
# Leave unset locally to disable reporting.
VITE_SENTRY_DSN=

# Optional: a version string so Sentry groups issues by release.
VITE_SENTRY_RELEASE=
